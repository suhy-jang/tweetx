import React from 'react';
import PropTypes from 'prop-types';

const Landing = props => {
  return (
    <div>
      loading ? background
      login ? feed
      logout ? splash
    </div>
  )
};

Landing.propTypes = {

};

export default Landing;
